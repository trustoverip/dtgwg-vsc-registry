## Why its own predicate

A fixture predicate used by the registry's own tests. It exercises every member of the definition format: an additional member with a local schema, a required `taskContext`, and a language-tagged label in two languages.
